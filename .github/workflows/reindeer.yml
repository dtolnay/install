name: reindeer

on:
  push:
    branches:
      - reindeer

jobs:
  build:
    uses: ./build.yml
    with:
      crate: reindeer
      git: facebookincubator/reindeer
    permissions:
      contents: write
    secrets: inherit
