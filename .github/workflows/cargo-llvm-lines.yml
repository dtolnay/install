name: cargo-llvm-lines

on:
  push:
    branches:
      - cargo-llvm-lines

jobs:
  build:
    uses: dtolnay/install/.github/workflows/build.yml@master
    with:
      crate: cargo-llvm-lines
    permissions:
      contents: write
    secrets: inherit
