name: xargo

on:
  push:
    branches:
      - xargo

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      crate: xargo
      bin: xargo-check
    permissions:
      contents: write
    secrets: inherit
