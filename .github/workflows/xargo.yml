name: xargo

on:
  push:
    branches:
      - xargo
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      crate: xargo
      bin: xargo-check
    permissions:
      contents: write
    secrets: inherit
