name: cargo-tally

on:
  push:
    branches:
      - cargo-tally

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      crate: cargo-tally
    permissions:
      contents: write
    secrets: inherit
