name: cargo-unlock

on:
  push:
    branches:
      - cargo-unlock

jobs:
  build:
    uses: dtolnay/install/.github/workflows/build.yml@master
    with:
      crate: cargo-unlock
    permissions:
      contents: write
    secrets: inherit
