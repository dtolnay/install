name: wasmtime-cli

on:
  push:
    branches:
      - wasmtime-cli
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      crate: wasmtime-cli
      bin: wasmtime
    permissions:
      attestations: write
      contents: write
      id-token: write
