name: cargo-bloat

on:
  push:
    branches:
      - cargo-bloat

jobs:
  build:
    uses: dtolnay/install/.github/workflows/build.yml@master
    with:
      crate: cargo-bloat
    permissions:
      contents: write
    secrets: inherit
