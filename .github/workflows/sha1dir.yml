name: sha1dir

on:
  push:
    branches:
      - sha1dir

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      crate: sha1dir
    permissions:
      contents: write
    secrets: inherit
