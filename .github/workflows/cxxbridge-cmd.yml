name: cxxbridge-cmd

on:
  push:
    branches:
      - cxxbridge-cmd

jobs:
  build:
    uses: ./build.yml
    with:
      crate: cxxbridge-cmd
      bin: cxxbridge
    permissions:
      contents: write
    secrets: inherit
