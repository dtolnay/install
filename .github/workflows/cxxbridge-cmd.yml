name: cxxbridge-cmd

on:
  push:
    branches:
      - cxxbridge-cmd

jobs:
  build:
    uses: dtolnay/install/.github/workflows/build.yml@master
    with:
      crate: cxxbridge-cmd
      bin: cxxbridge
    permissions:
      contents: write
    secrets:
      private_key: ${{secrets.private_key}}
